# El secreto solo lo recibe el proceso, nunca se pasa a la tool.
API_KEY=
JEV_PROVIDER=openrouter
# BASE_URL permite un endpoint compatible o proxy propio; normalmente se omite.
# BASE_URL=https://openrouter.ai/api
JEV_MODEL=jev-latest
